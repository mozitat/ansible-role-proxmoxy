galaxy_info:
  author: mozit.at
  description: >
    Configure proxmox hosts and lxc containers without dependencies.
     This role assumes proxmox is already installed on the host system.
  company: mozit.at
  license: MIT
  min_ansible_version: 2.0
  platforms:
  - name: Debian
    versions:
    - wheezy
    - jessie
    - bookworm

  galaxy_tags:
    - proxmox
    - system
    - lxc
    # - debian

dependencies: []
