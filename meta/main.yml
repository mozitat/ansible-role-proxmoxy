galaxy_info:
  author: mozit.eu
  description: >
    Configure proxmox hosts (4.x) and lxc containers without dependencies.
     This role assumes proxmox is already installed on the host system.
  company: mozit.eu
  license: MIT
  min_ansible_version: 2.0
  platforms:
  - name: Debian
    versions:
    - wheezy
    - jessie

  galaxy_tags:
    - proxmox
    - system
    - lxc
    # - debian

dependencies: []
